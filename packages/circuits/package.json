{
  "name": "circuits",
  "version": "1.0.0",
  "private": true,
  "description": "Circom circuits for ZK voting",
  "scripts": {
    "compile": "./scripts/compile.sh",
    "setup": "./scripts/setup.sh",
    "export-verifier": "./scripts/export-verifier.sh",
    "clean": "rm -rf build/*.wasm build/*.r1cs build/*.sym build/*.zkey build/*.json"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.5"
  },
  "devDependencies": {}
}

